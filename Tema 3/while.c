#include<stdio.h>

int main(){

    // while =  repete um código até até que a condição se torne falsa

    int a = 1;

    while (a <= 1000000)
    {
       printf("%d\n", a);
       a ++; // incremento = soma 1 à variável "a" sempre que o código roda
    }
    

    return 0;
}
